import { expect } from "chai";
import env from "env";
import {
    loadCredentials, authorizeCalendarApi, getAccessToken, listEvents
} from "../../src/app/authorize_calendar_api";

describe("authorizing calendar api", () => {
    // write tests here
});
